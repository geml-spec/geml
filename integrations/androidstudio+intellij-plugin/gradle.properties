# The IDE already ships a Kotlin stdlib. Bundling a second one is the classic
# way to get a NoSuchMethodError at runtime that no local build reproduces.
kotlin.stdlib.default.dependency=false

org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=768m
org.gradle.caching=true
