{
  "name": "@geml/geml",
  "version": "1.3.0",
  "publishConfig": {
    "access": "public"
  },
  "description": "Addressable, versioned plain-text docs for AI editing — geml get/set patches one #id block (~31x less context), geml history versions it, geml check fails the build on a broken reference. Reference parser & CLI for GEML.",
  "type": "module",
  "bin": {
    "geml": "dist/geml.js"
  },
  "main": "dist/geml.js",
  "types": "dist/geml.d.ts",
  "files": [
    "dist",
    "codemap",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=22"
  },
  "keywords": [
    "geml",
    "markup",
    "markdown",
    "parser",
    "cli",
    "document",
    "typed-block",
    "ai",
    "agent",
    "llm",
    "addressable",
    "versioning",
    "docs",
    "code-graph"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/geml-spec/geml.git",
    "directory": "geml-parser"
  },
  "homepage": "https://github.com/geml-spec/geml#readme",
  "bugs": {
    "url": "https://github.com/geml-spec/geml/issues"
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc && node test/all.mjs",
    "convert": "node dist/geml.js convert",
    "parse": "node dist/geml.js",
    "coverage": "tsc && c8 --all --include=dist/**/*.js --include=codemap/**/*.mjs --reporter=text --reporter=text-summary node test/all.mjs",
    "coverage:check": "tsc && c8 --all --include=dist/**/*.js --include=codemap/**/*.mjs --check-coverage --lines 95 --statements 95 --functions 97 --branches 90 node test/all.mjs",
    "prepublishOnly": "npm run build"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.19.21",
    "c8": "^10.1.3",
    "typescript": "^5.9.3"
  }
}
