=== meta
title = "GEML 推广路线图"
updated = "2026-07-13"
now = "第 1 步 · 发射前资产"
===

# GEML 推广路线图 {#top}

%% Dogfood:本文件就是一个 .geml,`geml check task.geml` 校验。只在 tracker 分支。
%% 按时间先后排——从上往下做。当前进行到 meta 的 `now` 字段所指那一步。

=== note {.legend}
一路从上往下推进。`[x]` = 已完成,`[ ]` = 待办;负责人:【你】= 你本人做,【我】= Claude 可独立做,【你+我】= 协作。发射文案见同分支的 `LAUNCH.md`。**当前在「第 1 步」。**
===

## 第 0 步 · 地基(已完成) {#done}

- [x] GEML 1.0 规范(中 + 英)+ 自举 dogfood
- [x] 参考实现 + 渲染器 + CLI,发布到 npm `@geml/geml`;agent 友好(`check`/`--json`/`fmt`/`export`/stdin)
- [x] 浏览器 playground 上线,math / mermaid / 图表 / 表格全部真渲染
- [x] README 按「可验证引用」楔子重定位;双协议(代码 MIT / 规范 CC-BY-4.0)
- [x] 治理:GOVERNANCE + GEP 流程 + issue 表单(GEP-0001 精简 aside 已合并)
- [x] GitHub Action(`geml check` 进 CI)
- [x] VS Code 扩展、Obsidian 插件(已构建,尚未上架)
- [x] tree-sitter 设计简报;《用你的语言写 GEML parser》指南 + 语言无关 fixtures
- [x] 测试覆盖 ~93% 行 + CI 覆盖率门槛(≥90);修掉列表拆分 bug
- [x] 楔子转向「可寻址 + 带版本的 AI 文档编辑」:`geml get/set <file> #id` 按块读写 + `geml revert`(新楔子的核心动词,已落 main)
- [x] geml-code-graph 杀手应用成型:`geml codemap build --root .` 自动认语言(TS/JS scip;Java/C/Py/Go/Kt Joern)、`serve` 默认目录 + 自动开浏览器、纯文档提交不重建;GEP-0003 `diagram {format=geml-code-graph src=…}`
- [x] README 大改(中英):hero 双线、「一份给程序员的礼物」章节、LLM 段以 get/set 领衔、仓库树与措辞更新
- [x] 仓库更名 geml-spec/geml + spec/ 目录重整,playground / 文档链接全量修正

## 第 1 步 · 发射前资产(现在) {#assets}

- [ ] 【你】录 15s GIF:playground 点「Break a reference」变红 → 存 `docs/demo.gif` → 【我】取消两份 README 图片注释 —— 唯一卡住视觉钩子的,约 15 分钟
- [ ] 【你+我】备选/加强视觉钩子:geml-code-graph 30s 演示(三行命令 → 整库调用图,秒开),工程师传播点
- [ ] 【我】旗舰博文——**已成稿待发**(`blog/flagship-{en,zh}.md`,转向后版:《改一行字,不该让 AI 先读完整份文档》/ *Your AI agent shouldn't read the whole file to change one line*);发出去才算完成
- [ ] 【我】Discord / Reddit 种子帖——**已成稿待发**(`seeds/seed-posts-{en,zh}.md`,分渠道角度);随发射铺出去才算完成

## 第 2 步 · 预热 → 发射 {#launch}

- [x] 【你+我】把 geml-code-graph 三行接入写进 `LAUNCH.md`(Show HN 首评 / 种子帖的工程师钩子)——2026-07-21,首评「bonus」段 + 独立「Engineer hook」节
- [ ] 【你】GIF 做完后,把 playground 丢进 Latent Space / LLM Devs Discord,收 2–3 条反馈
- [ ] 【你+我】拿一个真实 CI 接 geml-check-action,抓到一次坏引用 → 写成案例
- [ ] 【你】Show HN(周二~四 美东 8–9am;守评论区 4 小时;标题/首评在 `LAUNCH.md`)
- [ ] 【你】乘余热外扩:Lobsters → Reddit 错峰(r/LocalLLaMA、r/LLMDevs、r/programming)→ newsletters / awesome 列表

## 第 3 步 · 分发扩张(发射后,并行) {#distribution}

- [ ] 【你】发布 VS Code 扩展(注册 `geml-spec` publisher → `vsce publish`)
- [ ] 【你】提交 Obsidian 插件到社区商店
- [ ] 【你/我】把 geml-check-action 抽成独立 repo 并上 Marketplace
- [ ] 【贡献者 / 我】tree-sitter 外部扫描器语法(解锁 Neovim / Helix / Zed 高亮)
- [ ] 【你】GitHub Linguist 支持(等公开 `.geml` 文件够多再提,现在提会被拒)

## 并行轨道 · 标准化($0,随时可推) {#standards}

- [ ] 【你】招到第 2 个独立实现者(必须是另一个人)—— 用 parser 指南 + fixtures 当鱼饵
- [ ] 【你】凑齐约 5 个真实支持者 → 开 W3C 社区组(这是 go / no-go 门槛)

## 随时 · 技术零碎(不阻塞) {#misc}

- [ ] 【我】`geml check` 多文件 / glob 支持(让 Action 更干净)
- [x] 【你/我】`@geml/geml` **1.1.1 已发 npm**(2026-07-13;`--version` 双源同步、README logo 指向 docs/assets 新路径——顺带了结旧「1.0.1 补丁重发」)
