{
  "name": "geml",
  "version": "1.10.1",
  "description": "Block-addressed docs: read or replace one section of an existing Markdown file instead of loading the whole thing. Adds per-block history and a code-graph navigator.",
  "mcpServers": {
    "geml": {
      "command": "npx",
      "args": ["-y", "@geml/geml", "mcp", "--root", "."]
    }
  }
}
